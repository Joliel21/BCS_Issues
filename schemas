{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/bcs/viewer.schema.json",
  "title": "BCS Magazine viewer.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "title", "pages", "spreads"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "id": { "type": "string", "minLength": 1 },
    "title": { "type": "string", "minLength": 1 },
    "toc": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "pageNumber"],
        "properties": {
          "label": { "type": "string" },
          "pageNumber": { "type": "integer", "minimum": 1 }
        }
      },
      "default": []
    },
    "pages": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/page" }
    },
    "spreads": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/spread" }
    }
  },
  "$defs": {
    "page": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "pageNumber", "isNumbered", "elements", "fallbackText"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "pageNumber": { "type": ["integer", "null"], "minimum": 1 },
        "isNumbered": { "type": "boolean" },
        "fallbackText": { "type": "string" },
        "elements": {
          "type": "array",
          "items": { "$ref": "#/$defs/element" },
          "default": []
        }
      }
    },
    "spread": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "pageLeftId", "pageRightId", "pageLeftNumber", "pageRightNumber"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "pageLeftId": { "type": "string" },
        "pageRightId": { "type": "string" },
        "pageLeftNumber": { "type": ["integer", "null"], "minimum": 1 },
        "pageRightNumber": { "type": ["integer", "null"], "minimum": 1 }
      }
    },
    "element": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "style", "content"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "type": { "type": "string", "enum": ["text", "image"] },
        "linkUrl": { "type": ["string", "null"] },
        "style": {
          "type": "object",
          "additionalProperties": true,
          "required": ["x", "y", "w", "h"],
          "properties": {
            "x": { "type": "number", "minimum": 0, "maximum": 1 },
            "y": { "type": "number", "minimum": 0, "maximum": 1 },
            "w": { "type": "number", "minimum": 0, "maximum": 1 },
            "h": { "type": "number", "minimum": 0, "maximum": 1 }
          }
        },
        "content": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "text" } } },
          "then": {
            "properties": {
              "content": {
                "type": "object",
                "required": ["text"],
                "properties": { "text": { "type": "string" } }
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "image" } } },
          "then": {
            "properties": {
              "content": {
                "type": "object",
                "required": ["imageUrl"],
                "properties": { "imageUrl": { "type": "string" } }
              }
            }
          }
        }
      ]
    }
  }
}
